<!DOCTYPE html>
<html>
    <head>
        <title> Canciones</title>
    </head>
    <body>
        <h1>{{ seller }}</h1>
        <ul>
            {% for song in songs %}
                <li>{{ song.title }} - {{ song.price }}</li>
            {% endfor %}
        </ul>
</body>
</html>
